import BookingForm from '../components/BookingForm';

export default function Contact() {
    return (
        <div className="page-content" style={{ marginTop: '120px', display: 'flex', justifyContent: 'center' }}>
            <BookingForm />
        </div>
    );
}